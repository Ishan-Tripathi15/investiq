{
  "name": "investiq",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.12.4",
  "engines": { "node": ">=20.19.0" },
  "scripts": {
    "dev": "pnpm --parallel --filter @investiq/mobile --filter @investiq/api dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "prettier": "^3.6.2"
  }
}
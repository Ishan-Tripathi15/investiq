# Public API endpoint only. Never put private provider secrets in the mobile app.
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
# Required for Expo Push Service registration when using EAS.
EXPO_PUBLIC_EAS_PROJECT_ID=
